{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "options": {
        "env": {
            "DAGSTER_HOME": "${workspaceFolder}/",
            "RAW_DATA_VAULT": "${workspaceFolder}/data/raw"
        }
    },
    "tasks": [
        {
            "label": "data orchestration (dagster)",
            "type": "shell",
            "command": "poetry run dagster dev",
            "problemMatcher": []
        }
    ]
}